syntax="proto3";

message SampleMessage {
  oneof test_oneof {
    string name = 4;
    float sub_message = 9;
  }

  string otherName = 6;
  int32 number = 99;
}

service SearchService {
  rpc Search (SampleMessage) returns (SampleMessage);
}